# Runtime data — local to each machine, shouldn't be committed
*.db
*.db-wal
*.db-shm
*.sqb
*.sqb-wal
*.sqb-shm
admin_settings.json

# Admin login credentials for the /admin watchlist page — plaintext
# username:password, never committed
dbauth.txt

# Editor / OS cruft
*.swp
*.swo
.DS_Store

# Python
__pycache__/
*.pyc
venv/
.venv/

# Files delivered to chat by Claude (e.g. design-preview screenshots) get
# auto-saved here by the desktop app when this folder is connected — not
# part of the project itself, never committed
Claude outputs/
